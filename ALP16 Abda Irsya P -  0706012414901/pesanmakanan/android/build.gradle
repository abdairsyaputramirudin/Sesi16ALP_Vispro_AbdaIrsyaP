buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Tambahkan Google services plugin di sini
        classpath 'com.google.gms:google-services:4.4.2'  // Pastikan versinya sesuai atau terbaru
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
